<input type=hidden value="tel" data-def="type">
<div class="form-horizontal">
	<label for="title" class="control-label" style="margin-top:0;">请输入题目标题</label>
	<input type="text" name="title" data-def="title" class="form-control" value="<%= question.title %>">
    <div class="" style="font-weight:bold;margin-top: 20px;color: #767676;">
        <span class="glyphicon glyphicon-warning-sign" style="color: rgb(255, 155, 0);margin-right: 5px;"></span>
        该类题目必须放在第一题，且只用于验证国内微店卖家手机号
    </div>
</div>
<% if(config.brandnew) {%>
	<button class="btn btn-success add" data-dismiss="modal" data-action="add-question">添加</button>
<% } else { %>
    <button class="btn btn-primary update" data-dismiss="modal" data-action="update-<%=config.index%>">修改</button>
<% } %>